# Postgres connection string used by drizzle-kit and the runtime client.
# Local default assumes a Postgres on localhost:5432; swap for Neon/Supabase/etc.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/inlecture
