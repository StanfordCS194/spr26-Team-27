// Course index — the sidebar (rendered by the parent layout on mobile) is the
// page. Desktop shows an empty content area until a lecture is selected.
export default function LearnCourseIndex() {
  return null;
}
